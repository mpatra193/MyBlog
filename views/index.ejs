<%- include("partials/header.ejs") %>

<!-- First Parallax Image with Logo Text -->
<div class="bgimg-1 w3-display-container w3-opacity-min" id="home">
    <div class="w3-display-middle" style="white-space:nowrap;">
        <span class="w3-center w3-padding-large w3-black w3-xlarge w3-wide w3-animate-opacity">MY <span
                class="w3-xlarge">BLOG</span>
    </div>
</div>
  




<!-- simple code for delete and put -->
<!-- <div>
    <ul class="list-group list-group-flush border">   
    <%# posts.forEach( (post) =>{ %>
        <a href="/posts/<%#= post.id %>" style="text-decoration: none"><li class="list-group-item list-group-item-action"><%#= post.title %></li></a>
        <form action="/posts/<%#= post.id %>?_method=DELETE" method="POST">
            <button type="submit">Delete</button>
        </form>
        <a href="/posts/<%#= post.id %>/edit">Edit</a>
    <%# }); %>
    </ul> 
</div> -->

<% if (posts[0]) { %>
  <h1 class="text-center m-4 text-dark-50">The Latest Post</h1>
<% } %> 

<div class="row row-cols-1 row-cols-md-3 g-4 mt-3 mb-5 text-black-50">

    <% posts.forEach( (post,index) =>{ %>
      
        
         
        <div class="col">
            <div class="card bg-dark-subtle" style="max-width: 640px;">
                <div class="row g-0">
                  <div class="col-md-4">
                    <img src="<%= post.image %>" class="img-fluid rounded-start" style="width: 100%; height: 120px; object-fit: cover;">
                  </div>
                  <div class="col-md-8">
                    <div class="card-body">
                      <h5 class="card-title"><%= post.title %></h5>
                      
                      <!-- Button trigger modal -->
                      
                      <button type="button" class="btn btn-primary bg-" data-bs-toggle="modal" data-bs-target="#modal-<%= post.id %>">
                          Open
                      </button>
                      
      
                      <!-- Modal -->
                      <div class="modal fade" id="modal-<%= post.id %>" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="modalLabel-<%= post.id %> aria-hidden="true">
                        <div class="modal-dialog modal-dialog-scrollable modal-dialog-centered">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h5 class="modal-title" id="modalLabel-<%= post.id %>"><%= post.title %></h5>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                </div>
                                <div class="modal-body">
                                    <%= post.content %>
                                </div>
                                <div class="modal-footer">
                                    <form action="/posts/<%= post.id %>?_method=DELETE" method="POST">
                                        <button type="submit" class="btn btn-secondary">Delete</button>
                                    </form>
                                    <a href="/posts/<%= post.id %>/edit"><button type="button" class="btn btn-primary">Edit</button></a>    
                                </div>
                            </div>
                        </div>
                      </div>
                      <p class="card-text">
                        <small class="text-black-50"><%=new Date().getDate()%>th <%= new Date().toLocaleString('default', { month: 'short' }) %>,<%= new Date().getFullYear() %></small>
                      </p>
                    </div>
                  </div>
                </div>
            </div>
        </div>
    <% }); %>
</div>

<div class="row flex-column align-content-center rounded-circle mt-6">
  <a href="/add_post" class="col-1 d-flex flex-row justify-content-center">
    <button type="button" class="btn btn-dark">
      <div>
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" class="bi bi-plus" viewBox="0 0 16 16">
          <path d="M8 4a.5.5 0 0 1 .5.5v3h3a.5.5 0 0 1 0 1h-3v3a.5.5 0 0 1-1 0v-3h-3a.5.5 0 0 1 0-1h3v-3A.5.5 0 0 1 8 4"/>
        </svg>
      </div>
    </button>
  </a>
</div>
<h4 class="text-center">create a new post</h4>

 <%# posts.forEach( (post) =>{ %> 
    <!-- <div class="card" style="max-width: 640px;">
        <div class="row g-0">
          <div class="col-md-4">
            <img src="/images/cards/3.jpg" class="img-fluid rounded-start">
          </div> -->
          <!-- <div class="col-md-8">
            <div class="card-body">
              <h5 class="card-title"><%# post.title %></h5>
              <p class="card-text">
                This is a wider card with supporting text below as a natural lead-in 
                to additional content. This content is a little bit longer.
              </p> -->
              <!-- <p class="card-text"> -->
                <!-- <small class="text-muted"><%# new Date().getDate()%>th <%# new Date().getMonth() + 1 %>,<%# new Date().getFullYear() %></small> -->
              <!-- </p>
            </div>
          </div>
        </div>
    </div> --> 
    
<%# }); %> 

<%- include("partials/footer.ejs") %>


    